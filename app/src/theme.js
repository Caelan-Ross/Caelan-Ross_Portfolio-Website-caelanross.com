import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: 'Muli, sans-serif',
    h1: { fontFamily: '"Space Mono", monospace' },
    h2: { fontFamily: '"Space Mono", monospace' },
    h3: { fontFamily: '"Space Mono", monospace' },
    h4: { fontFamily: '"Space Mono", monospace' },
    h5: { fontFamily: '"Space Mono", monospace' },
    h6: { fontFamily: '"Space Mono", monospace' },
  },
});

export default theme;
